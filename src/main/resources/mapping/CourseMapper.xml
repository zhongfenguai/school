<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dfrz.mapper.CourseMapper">
    <select id="getCourseStudentCountByCid" resultType="int">
SELECT COUNT(*) FROM course_student WHERE courseid=#{courseid}
    </select>
</mapper>